apiVersion: v1 
kind: ReplicaSet
metadata: 
  name: demo-app 
spec: 
  replicas: 1
  template: 
    metadata: 
      labels:
        app: demo-app

  spec: 
    containers: 
    - name: demo-app
      image: nginx 

apiversion: v2
kind: Vishnu

apiversion: V2
kind: making changes on git-hub web browser

apiversion: v3
kind: making changes to check what is fetch command
